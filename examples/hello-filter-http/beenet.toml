schema_version = 1

[task]
name = "hello-filter-http"
version = "0.1.0"
interface = "fermyon:spin/inbound-http"

[runtime]
max_memory_mb = 64
deadline_ms = 5000

[networking]
allowed_outbound_hosts = []

[audit]
cpu_reporting = "wall"
