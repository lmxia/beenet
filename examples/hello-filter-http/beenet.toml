schema_version = 1

[task]
name = "hello-filter-http"
version = "0.1.0"
interface = "wasi:http/incoming-handler@0.2"

[runtime]
max_memory_mb = 64
deadline_ms = 5000

[networking]
allowed_outbound_hosts = []

[audit]
cpu_reporting = "wall"
