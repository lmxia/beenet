# Shared Alibaba Cloud ALB entrypoint for the Beenet HTTP control plane.
# Operations UI is provided by Beenet Cloud Console; the Registry status API remains available internally.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: beenet-control-plane
  namespace: beenet
spec:
  ingressClassName: alb
  rules:
    - host: registry.hyperos.online
      http:
        paths:
          - path: /v1/workers
            pathType: Prefix
            backend:
              service:
                name: beenet-registry
                port:
                  number: 3030
          - path: /v1/gateways
            pathType: Prefix
            backend:
              service:
                name: beenet-registry
                port:
                  number: 3030
          - path: /health
            pathType: Exact
            backend:
              service:
                name: beenet-registry
                port:
                  number: 3030
    - host: frontdoor.hyperos.online
      http:
        paths:
          - path: /run/ipfs
            pathType: Prefix
            backend:
              service:
                name: beenet-frontdoor
                port:
                  number: 80
          - path: /health
            pathType: Exact
            backend:
              service:
                name: beenet-frontdoor
                port:
                  number: 80
