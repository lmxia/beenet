apiVersion: v1
kind: Service
metadata:
  name: {{ include "beenet-registry.fullname" . }}
  labels:
    {{- include "beenet-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "beenet-registry.selectorLabels" . | nindent 4 }}
