apiVersion: v2
name: beenet-registry
description: >
  Beenet worker registry — HTTP control plane for worker join/heartbeat
  with Redis-backed registration persistence.
type: application
version: 0.1.0
appVersion: "0.1.0"
