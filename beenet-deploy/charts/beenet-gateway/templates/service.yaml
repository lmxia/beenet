apiVersion: v1
kind: Service
metadata:
  name: {{ include "beenet-gateway.fullname" . }}
  labels:
    {{- include "beenet-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "beenet-gateway.selectorLabels" . | nindent 4 }}
